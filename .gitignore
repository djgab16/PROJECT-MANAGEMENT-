# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sw?

# Solution files are tracked on purpose. `*.sln` was inherited from the Vite template, which
# assumes the repo contains no .NET projects; here the solution is the single build entry point
# for the API and its test project, so ignoring it would hide it from every contributor and CI.
!*.sln
